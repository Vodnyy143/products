<Window x:Class="products.Views.OrderEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:products.Views"
        mc:Ignorable="d"
        Title="OrderEditWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding WindowTitle}"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Артикул заказа: *"/>
            <TextBox Grid.Row="0" Grid.Column="1"
                     Text="{Binding Order.Article, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Статус заказа:"/>
            <ComboBox Grid.Row="1" Grid.Column="1"
                      ItemsSource="{Binding Statuses}"
                      SelectedValue="{Binding Order.StatusId}"
                      SelectedValuePath="Id"
                      DisplayMemberPath="Name"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Адрес пункта выдачи: *"/>
            <ComboBox Grid.Row="2" Grid.Column="1"
                      ItemsSource="{Binding PickupPoints}"
                      SelectedValue="{Binding Order.PickupPointId}"
                      SelectedValuePath="Id"
                      DisplayMemberPath="Address"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Дата заказа: *"/>
            <DatePicker Grid.Row="3" Grid.Column="1"
                        SelectedDate="{Binding Order.OrderDate}"
                        Margin="3"/>

            <Label Grid.Row="4" Grid.Column="0" Content="Дата доставки:"/>
            <DatePicker Grid.Row="4" Grid.Column="1"
                        SelectedDate="{Binding Order.DeliveryDate}"
                        Margin="3"/>
        </Grid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Отмена"
                    Command="{Binding CancelCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Background="#FFCCCC"
                    MinWidth="100" Height="35"/>
            <Button Content="💾 Сохранить"
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Background="#00FA9A"
                    FontWeight="Bold"
                    MinWidth="120" Height="35"
                    IsDefault="True"
                    Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
